import { Stack, Typography } from '@mui/material'
import theme from 'styles/theme'

const HeaderNav = () => {
  return (
    <Stack sx={{ backgroundColor: theme.palette.primary.main }}>
      <Typography>HeaderNav</Typography>
    </Stack>
  )
}

export default HeaderNav
